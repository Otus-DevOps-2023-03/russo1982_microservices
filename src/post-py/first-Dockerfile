FROM python:3.6.0-alpine

WORKDIR /app
ADD . /app
#COPY requirements.txt /app
#RUN pip install -r /app/requirements.txt
COPY requirements.txt /app
RUN apk --no-cache --update add build-base && pip install thriftpy2
#RUN apk update && apk upgrade && pip install thriftpy cython
#RUN apk --no-cache update && apk upgrade && apk add snappy && pip install thriftpy cython
RUN pip install -r /app/requirements.txt
#    && apk del build-base

ENV POST_DATABASE_HOST post_db
ENV POST_DATABASE posts

#ENTRYPOINT ["python3", "post_app.py"]
CMD ["python3", "post_app.py"]
